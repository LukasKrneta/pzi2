{
  "name": "pzi2",
  "version": "1.0.0",
  "description": "Get started by customizing your environment (defined in the .idx/dev.nix file) with the tools and IDE extensions you'll need for your project!",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --experimental-sqlite ./server.js --port 9002 --host 0.0.0.0 --disable-host-check"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mime-types": "^3.0.1"
  }
}
